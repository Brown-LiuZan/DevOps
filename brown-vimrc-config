""" Brown's settings
""" This file is intended to be renamed into .vimrc and placed under user's home directory.

"
autocmd BufWritePre * :%s/\s\+$//e

" Window visualization
set number
set textwidth=120
" set colorcolumn=121
let &colorcolumn="81,".join(range(121, 255), ",")
set hlsearch
set ruler " show (line, column) 
syntax on
color default
" color delek

" Indent
set tabstop=4
set softtabstop=4
set shiftwidth=4
set expandtab
set autoindent
" set smartindent
